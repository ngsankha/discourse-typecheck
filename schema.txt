GroupManager didn't work
SiteSetting has the following schema:
     id => Integer
     name => String
     data_type => Integer
     value => Text
     created_at => Datetime
     updated_at => Datetime
     __associations => {  }
TopicLink has the following schema:
     id => Integer
     topic_id => Integer
     post_id => Integer
     user_id => Integer
     url => String
     domain => String
     internal => true or false
     link_topic_id => Integer
     created_at => Datetime
     updated_at => Datetime
     reflection => true or false
     clicks => Integer
     link_post_id => Integer
     title => String
     crawled_at => Datetime
     quote => true or false
     extension => String
     __associations => { belongs_to: :topic or :user or :post or :link_topic or :link_post, has_many: :topic_link_clicks }
UserAction has the following schema:
     id => Integer
     action_type => Integer
     user_id => Integer
     target_topic_id => Integer
     target_post_id => Integer
     target_user_id => Integer
     acting_user_id => Integer
     created_at => Datetime
     updated_at => Datetime
     queued_post_id => Integer
     __associations => { belongs_to: :user or :target_post or :target_topic }
Post has the following schema:
     id => Integer
     user_id => Integer
     topic_id => Integer
     post_number => Integer
     raw => Text
     cooked => Text
     created_at => Datetime
     updated_at => Datetime
     reply_to_post_number => Integer
     reply_count => Integer
     quote_count => Integer
     deleted_at => Datetime
     off_topic_count => Integer
     like_count => Integer
     incoming_link_count => Integer
     bookmark_count => Integer
     avg_time => Integer
     score => Float
     reads => Integer
     post_type => Integer
     vote_count => Integer
     sort_order => Integer
     last_editor_id => Integer
     hidden => true or false
     hidden_reason_id => Integer
     notify_moderators_count => Integer
     spam_count => Integer
     illegal_count => Integer
     inappropriate_count => Integer
     last_version_at => Datetime
     user_deleted => true or false
     reply_to_user_id => Integer
     percent_rank => Float
     notify_user_count => Integer
     like_score => Integer
     deleted_by_id => Integer
     edit_reason => String
     word_count => Integer
     version => Integer
     cook_method => Integer
     wiki => true or false
     baked_at => Datetime
     baked_version => Integer
     hidden_at => Datetime
     self_edits => Integer
     reply_quoted => true or false
     via_email => true or false
     raw_email => Text
     public_version => Integer
     action_code => String
     image_url => String
     __associations => { belongs_to: :deleted_by or :user or :topic or :reply_to_user, has_one: :post_search_data or :post_stat or :incoming_email, has_many: :_custom_fields or :post_replies or :replies or :post_actions or :topic_links or :group_mentions or :post_uploads or :uploads or :post_details or :post_revisions or :revisions or :user_actions }
Notification has the following schema:
     id => Integer
     notification_type => Integer
     user_id => Integer
     data => String
     read => true or false
     created_at => Datetime
     updated_at => Datetime
     topic_id => Integer
     post_number => Integer
     post_action_id => Integer
     __associations => { belongs_to: :user or :topic }
ApplicationRequest has the following schema:
     id => Integer
     date => Date
     req_type => Integer
     count => Integer
     __associations => {  }
Group has the following schema:
     id => Integer
     name => String
     created_at => Datetime
     updated_at => Datetime
     automatic => true or false
     user_count => Integer
     automatic_membership_email_domains => Text
     automatic_membership_retroactive => true or false
     primary_group => true or false
     title => String
     grant_trust_level => Integer
     incoming_email => String
     has_messages => true or false
     flair_url => String
     flair_bg_color => String
     flair_color => String
     bio_raw => Text
     bio_cooked => Text
     allow_membership_requests => true or false
     full_name => String
     default_notification_level => Integer
     visibility_level => Integer
     public_exit => true or false
     public_admission => true or false
     membership_request_template => Text
     messageable_level => Integer
     mentionable_level => Integer
     __associations => { has_many: :_custom_fields or :category_groups or :group_users or :group_mentions or :group_archived_messages or :categories or :users or :group_histories, has_and_belongs_to_many: :web_hooks }
Group::HABTM_WebHooks has the following schema:
     web_hook_id => Integer
     group_id => Integer
     __associations => { belongs_to: :left_side or :web_hook }
QueuedPost has the following schema:
     id => Integer
     queue => String
     state => Integer
     user_id => Integer
     raw => Text
     post_options => Json
     topic_id => Integer
     approved_by_id => Integer
     approved_at => Datetime
     rejected_by_id => Integer
     rejected_at => Datetime
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :user or :topic or :approved_by or :rejected_by }
EmailToken has the following schema:
     id => Integer
     user_id => Integer
     email => String
     token => String
     confirmed => true or false
     expired => true or false
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :user }
User has the following schema:
     id => Integer
     username => String
     created_at => Datetime
     updated_at => Datetime
     name => String
     seen_notification_id => Integer
     last_posted_at => Datetime
     password_hash => String
     salt => String
     active => true or false
     username_lower => String
     last_seen_at => Datetime
     admin => true or false
     last_emailed_at => Datetime
     trust_level => Integer
     approved => true or false
     approved_by_id => Integer
     approved_at => Datetime
     previous_visit_at => Datetime
     suspended_at => Datetime
     suspended_till => Datetime
     date_of_birth => Date
     views => Integer
     flag_level => Integer
     ip_address => Inet
     moderator => true or false
     title => String
     uploaded_avatar_id => Integer
     locale => String
     primary_group_id => Integer
     registration_ip_address => Inet
     staged => true or false
     first_seen_at => Datetime
     silenced_till => Datetime
     group_locked_trust_level => Integer
     manual_locked_trust_level => Integer
     __associations => { has_one: :user_search_data or :primary_email or :user_option or :user_avatar or :facebook_user_info or :twitter_user_info or :github_user_info or :google_user_info or :oauth2_user_info or :user_stat or :user_profile or :single_sign_on_record or :api_key, has_many: :_custom_fields or :posts or :notifications or :topic_users or :category_users or :tag_users or :user_api_keys or :topics or :user_open_ids or :user_actions or :post_actions or :user_badges or :badges or :email_logs or :incoming_emails or :post_timings or :topic_allowed_users or :topics_allowed or :email_tokens or :user_visits or :invites or :topic_links or :uploads or :user_warnings or :user_archived_messages or :email_change_requests or :directory_items or :user_auth_tokens or :user_emails or :group_users or :groups or :secure_categories or :muted_user_records or :muted_users or :acting_group_histories or :targeted_group_histories, belongs_to: :approved_by or :primary_group or :uploaded_avatar }
Category has the following schema:
     id => Integer
     name => String
     color => String
     topic_id => Integer
     topic_count => Integer
     created_at => Datetime
     updated_at => Datetime
     user_id => Integer
     topics_year => Integer
     topics_month => Integer
     topics_week => Integer
     slug => String
     description => Text
     text_color => String
     read_restricted => true or false
     auto_close_hours => Float
     post_count => Integer
     latest_post_id => Integer
     latest_topic_id => Integer
     position => Integer
     parent_category_id => Integer
     posts_year => Integer
     posts_month => Integer
     posts_week => Integer
     email_in => String
     email_in_allow_strangers => true or false
     topics_day => Integer
     posts_day => Integer
     allow_badges => true or false
     name_lower => String
     auto_close_based_on_last_post => true or false
     topic_template => Text
     suppress_from_homepage => true or false
     contains_messages => true or false
     sort_order => String
     sort_ascending => true or false
     uploaded_logo_id => Integer
     uploaded_background_id => Integer
     topic_featured_link_allowed => true or false
     all_topics_wiki => true or false
     show_subcategory_list => true or false
     num_featured_topics => Integer
     default_view => String
     subcategory_list_style => String
     default_top_period => String
     mailinglist_mirror => true or false
     __associations => { has_one: :category_search_data, has_many: :_custom_fields or :topics or :category_users or :category_featured_topics or :featured_topics or :category_featured_users or :featured_users or :category_groups or :groups or :subcategories or :category_tags or :tags or :category_tag_groups or :tag_groups, belongs_to: :topic or :topic_only_relative_url or :user or :latest_post or :uploaded_logo or :uploaded_background or :parent_category, has_and_belongs_to_many: :web_hooks }
Category::HABTM_WebHooks has the following schema:
     web_hook_id => Integer
     category_id => Integer
     __associations => { belongs_to: :left_side or :web_hook }
TopTopic has the following schema:
     id => Integer
     topic_id => Integer
     yearly_posts_count => Integer
     yearly_views_count => Integer
     yearly_likes_count => Integer
     monthly_posts_count => Integer
     monthly_views_count => Integer
     monthly_likes_count => Integer
     weekly_posts_count => Integer
     weekly_views_count => Integer
     weekly_likes_count => Integer
     daily_posts_count => Integer
     daily_views_count => Integer
     daily_likes_count => Integer
     daily_score => Float
     weekly_score => Float
     monthly_score => Float
     yearly_score => Float
     all_score => Float
     daily_op_likes_count => Integer
     weekly_op_likes_count => Integer
     monthly_op_likes_count => Integer
     yearly_op_likes_count => Integer
     quarterly_posts_count => Integer
     quarterly_views_count => Integer
     quarterly_likes_count => Integer
     quarterly_score => Float
     quarterly_op_likes_count => Integer
     __associations => { belongs_to: :topic }
Topic has the following schema:
     id => Integer
     title => String
     last_posted_at => Datetime
     created_at => Datetime
     updated_at => Datetime
     views => Integer
     posts_count => Integer
     user_id => Integer
     last_post_user_id => Integer
     reply_count => Integer
     featured_user1_id => Integer
     featured_user2_id => Integer
     featured_user3_id => Integer
     avg_time => Integer
     deleted_at => Datetime
     highest_post_number => Integer
     image_url => String
     like_count => Integer
     incoming_link_count => Integer
     category_id => Integer
     visible => true or false
     moderator_posts_count => Integer
     closed => true or false
     archived => true or false
     bumped_at => Datetime
     has_summary => true or false
     vote_count => Integer
     archetype => String
     featured_user4_id => Integer
     notify_moderators_count => Integer
     spam_count => Integer
     pinned_at => Datetime
     score => Float
     percent_rank => Float
     subtype => String
     slug => String
     deleted_by_id => Integer
     participant_count => Integer
     word_count => Integer
     excerpt => String
     pinned_globally => true or false
     pinned_until => Datetime
     fancy_title => String
     highest_staff_post_number => Integer
     featured_link => String
     __associations => { has_many: :_custom_fields or :category_users or :posts or :ordered_posts or :topic_allowed_users or :topic_allowed_groups or :group_archived_messages or :user_archived_messages or :allowed_groups or :allowed_group_users or :allowed_users or :queued_posts or :topic_tags or :tags or :tag_users or :topic_users or :topic_links or :topic_invites or :invites or :topic_timers, belongs_to: :deleted_by or :category or :user or :last_poster or :featured_user1 or :featured_user2 or :featured_user3 or :featured_user4, has_one: :topic_search_data or :top_topic or :user_warning or :first_post or :topic_embed }
PostAction has the following schema:
     id => Integer
     post_id => Integer
     user_id => Integer
     post_action_type_id => Integer
     deleted_at => Datetime
     created_at => Datetime
     updated_at => Datetime
     deleted_by_id => Integer
     related_post_id => Integer
     staff_took_action => true or false
     deferred_by_id => Integer
     targets_topic => true or false
     agreed_at => Datetime
     agreed_by_id => Integer
     deferred_at => Datetime
     disagreed_at => Datetime
     disagreed_by_id => Integer
     __associations => { belongs_to: :deleted_by or :post or :user or :post_action_type or :related_post or :target_user }
PostRevision has the following schema:
     id => Integer
     user_id => Integer
     post_id => Integer
     modifications => Text
     number => Integer
     created_at => Datetime
     updated_at => Datetime
     hidden => true or false
     __associations => { belongs_to: :post or :user }
TopicUser has the following schema:
     user_id => Integer
     topic_id => Integer
     posted => true or false
     last_read_post_number => Integer
     highest_seen_post_number => Integer
     last_visited_at => Datetime
     first_visited_at => Datetime
     notification_level => Integer
     notifications_changed_at => Datetime
     notifications_reason_id => Integer
     total_msecs_viewed => Integer
     cleared_pinned_at => Datetime
     id => Integer
     last_emailed_post_number => Integer
     liked => true or false
     bookmarked => true or false
     __associations => { belongs_to: :user or :topic }
UserField has the following schema:
     id => Integer
     name => String
     field_type => String
     created_at => Datetime
     updated_at => Datetime
     editable => true or false
     description => String
     required => true or false
     show_on_profile => true or false
     position => Integer
     show_on_user_card => true or false
     __associations => { has_many: :user_field_options }
PostActionType has the following schema:
     name_key => String
     is_flag => true or false
     icon => String
     created_at => Datetime
     updated_at => Datetime
     id => Integer
     position => Integer
     __associations => {  }
WebHook has the following schema:
     id => Integer
     payload_url => String
     content_type => Integer
     last_delivery_status => Integer
     status => Integer
     secret => String
     wildcard_web_hook => true or false
     verify_certificate => true or false
     active => true or false
     created_at => Datetime
     updated_at => Datetime
     __associations => { has_and_belongs_to_many: :web_hook_event_types or :groups or :categories, has_many: :web_hook_events }
WebHook::HABTM_WebHookEventTypes has the following schema:
     web_hook_id => Integer
     web_hook_event_type_id => Integer
     __associations => { belongs_to: :left_side or :web_hook_event_type }
WebHook::HABTM_Groups has the following schema:
     web_hook_id => Integer
     group_id => Integer
     __associations => { belongs_to: :left_side or :group }
WebHook::HABTM_Categories has the following schema:
     web_hook_id => Integer
     category_id => Integer
     __associations => { belongs_to: :left_side or :category }
ApiKey has the following schema:
     id => Integer
     key => String
     user_id => Integer
     created_by_id => Integer
     created_at => Datetime
     updated_at => Datetime
     allowed_ips => Inet
     hidden => true or false
     __associations => { belongs_to: :user or :created_by }
Badge has the following schema:
     id => Integer
     name => String
     description => Text
     badge_type_id => Integer
     grant_count => Integer
     created_at => Datetime
     updated_at => Datetime
     allow_title => true or false
     multiple_grant => true or false
     icon => String
     listable => true or false
     target_posts => true or false
     query => Text
     enabled => true or false
     auto_revoke => true or false
     badge_grouping_id => Integer
     trigger => Integer
     show_posts => true or false
     system => true or false
     image => String
     long_description => Text
     __associations => { belongs_to: :badge_type or :badge_grouping, has_many: :user_badges }
BadgeGrouping has the following schema:
     id => Integer
     name => String
     description => Text
     position => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { has_many: :badges }
BadgeType has the following schema:
     id => Integer
     name => String
     created_at => Datetime
     updated_at => Datetime
     __associations => { has_many: :badges }
CategoryCustomField has the following schema:
     id => Integer
     category_id => Integer
     name => String
     value => Text
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :category }
CategoryFeaturedTopic has the following schema:
     category_id => Integer
     topic_id => Integer
     created_at => Datetime
     updated_at => Datetime
     rank => Integer
     id => Integer
     __associations => { belongs_to: :category or :topic }
CategoryFeaturedUser has the following schema:
     id => Integer
     category_id => Integer
     user_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :category or :user }
CategoryGroup has the following schema:
     id => Integer
     category_id => Integer
     group_id => Integer
     created_at => Datetime
     updated_at => Datetime
     permission_type => Integer
     __associations => { belongs_to: :category or :group }
CategorySearchData has the following schema:
     category_id => Integer
     search_data => Tsvector
     raw_data => Text
     locale => Text
     version => Integer
     __associations => { belongs_to: :category }
CategoryTag has the following schema:
     id => Integer
     category_id => Integer
     tag_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :category or :tag }
CategoryTagGroup has the following schema:
     id => Integer
     category_id => Integer
     tag_group_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :category or :tag_group }
CategoryUser has the following schema:
     id => Integer
     category_id => Integer
     user_id => Integer
     notification_level => Integer
     __associations => { belongs_to: :category or :user }
ChildTheme has the following schema:
     id => Integer
     parent_theme_id => Integer
     child_theme_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :parent_theme or :child_theme }
ColorScheme has the following schema:
     id => Integer
     name => String
     version => Integer
     created_at => Datetime
     updated_at => Datetime
     via_wizard => true or false
     base_scheme_id => String
     theme_id => Integer
     __associations => { has_many: :color_scheme_colors, belongs_to: :theme }
ColorSchemeColor has the following schema:
     id => Integer
     name => String
     hex => String
     color_scheme_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :color_scheme }
CustomEmoji has the following schema:
     id => Integer
     name => String
     upload_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :upload }
Developer has the following schema:
     id => Integer
     user_id => Integer
     __associations => { belongs_to: :user }
DirectoryItem has the following schema:
     id => Integer
     period_type => Integer
     user_id => Integer
     likes_received => Integer
     likes_given => Integer
     topics_entered => Integer
     topic_count => Integer
     post_count => Integer
     created_at => Datetime
     updated_at => Datetime
     days_visited => Integer
     posts_read => Integer
     __associations => { belongs_to: :user, has_one: :user_stat }
Draft has the following schema:
     id => Integer
     user_id => Integer
     draft_key => String
     data => Text
     created_at => Datetime
     updated_at => Datetime
     sequence => Integer
     revisions => Integer
     __associations => {  }
DraftSequence has the following schema:
     id => Integer
     user_id => Integer
     draft_key => String
     sequence => Integer
     __associations => {  }
EmailChangeRequest has the following schema:
     id => Integer
     user_id => Integer
     old_email => String
     new_email => String
     old_email_token_id => Integer
     new_email_token_id => Integer
     change_state => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :old_email_token or :new_email_token or :user }
EmailLog has the following schema:
     id => Integer
     to_address => String
     email_type => String
     user_id => Integer
     created_at => Datetime
     updated_at => Datetime
     reply_key => String
     post_id => Integer
     topic_id => Integer
     skipped => true or false
     skipped_reason => String
     bounce_key => String
     bounced => true or false
     message_id => String
     __associations => { belongs_to: :user or :post or :topic }
EmbeddableHost has the following schema:
     id => Integer
     host => String
     category_id => Integer
     created_at => Datetime
     updated_at => Datetime
     path_whitelist => String
     class_name => String
     __associations => { belongs_to: :category }
FacebookUserInfo has the following schema:
     id => Integer
     user_id => Integer
     facebook_user_id => Integer
     username => String
     first_name => String
     last_name => String
     email => String
     gender => String
     name => String
     link => String
     created_at => Datetime
     updated_at => Datetime
     avatar_url => String
     about_me => Text
     location => String
     website => Text
     __associations => { belongs_to: :user }
GithubUserInfo has the following schema:
     id => Integer
     user_id => Integer
     screen_name => String
     github_user_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :user }
GivenDailyLike has the following schema:
     user_id => Integer
     likes_given => Integer
     given_date => Date
     limit_reached => true or false
     __associations => { belongs_to: :user }
GoogleUserInfo has the following schema:
     id => Integer
     user_id => Integer
     google_user_id => String
     first_name => String
     last_name => String
     email => String
     gender => String
     name => String
     link => String
     profile_link => String
     picture => String
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :user }
GroupArchivedMessage has the following schema:
     id => Integer
     group_id => Integer
     topic_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :user or :topic }
GroupCustomField has the following schema:
     id => Integer
     group_id => Integer
     name => String
     value => Text
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :group }
GroupHistory has the following schema:
     id => Integer
     group_id => Integer
     acting_user_id => Integer
     target_user_id => Integer
     action => Integer
     subject => String
     prev_value => Text
     new_value => Text
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :group or :acting_user or :target_user }
GroupMention has the following schema:
     id => Integer
     post_id => Integer
     group_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :post or :group }
GroupUser has the following schema:
     id => Integer
     group_id => Integer
     user_id => Integer
     created_at => Datetime
     updated_at => Datetime
     owner => true or false
     notification_level => Integer
     __associations => { belongs_to: :group or :user }
IncomingDomain has the following schema:
     id => Integer
     name => String
     https => true or false
     port => Integer
     __associations => {  }
IncomingEmail has the following schema:
     id => Integer
     user_id => Integer
     topic_id => Integer
     post_id => Integer
     raw => Text
     error => Text
     message_id => Text
     from_address => Text
     to_addresses => Text
     cc_addresses => Text
     subject => Text
     created_at => Datetime
     updated_at => Datetime
     rejection_message => Text
     is_auto_generated => true or false
     is_bounce => true or false
     __associations => { belongs_to: :user or :topic or :post }
IncomingLink has the following schema:
     id => Integer
     created_at => Datetime
     user_id => Integer
     ip_address => Inet
     current_user_id => Integer
     post_id => Integer
     incoming_referer_id => Integer
     __associations => { belongs_to: :post or :user or :incoming_referer }
IncomingReferer has the following schema:
     id => Integer
     path => String
     incoming_domain_id => Integer
     __associations => { belongs_to: :incoming_domain }
InstagramUserInfo has the following schema:
     id => Integer
     user_id => Integer
     screen_name => String
     instagram_user_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :user }
Invite has the following schema:
     id => Integer
     invite_key => String
     email => String
     invited_by_id => Integer
     user_id => Integer
     redeemed_at => Datetime
     created_at => Datetime
     updated_at => Datetime
     deleted_at => Datetime
     deleted_by_id => Integer
     invalidated_at => Datetime
     moderator => true or false
     custom_message => Text
     __associations => { belongs_to: :deleted_by or :user or :topic or :invited_by, has_many: :invited_groups or :groups or :topic_invites or :topics }
InvitedGroup has the following schema:
     id => Integer
     group_id => Integer
     invite_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :group or :invite }
MutedUser has the following schema:
     id => Integer
     user_id => Integer
     muted_user_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :user or :muted_user }
Oauth2UserInfo has the following schema:
     id => Integer
     user_id => Integer
     uid => String
     provider => String
     email => String
     name => String
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :user }
OnceoffLog has the following schema:
     id => Integer
     job_name => String
     created_at => Datetime
     updated_at => Datetime
     __associations => {  }
OptimizedImage has the following schema:
     id => Integer
     sha1 => String
     extension => String
     width => Integer
     height => Integer
     upload_id => Integer
     url => String
     __associations => { belongs_to: :upload }
Permalink has the following schema:
     id => Integer
     url => String
     topic_id => Integer
     post_id => Integer
     category_id => Integer
     created_at => Datetime
     updated_at => Datetime
     external_url => String
     __associations => { belongs_to: :topic or :post or :category }
PluginStoreRow has the following schema:
     id => Integer
     plugin_name => String
     key => String
     type_name => String
     value => Text
     __associations => {  }
PostCustomField has the following schema:
     id => Integer
     post_id => Integer
     name => String
     value => Text
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :post }
PostDetail has the following schema:
     id => Integer
     post_id => Integer
     key => String
     value => String
     extra => Text
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :post }
PostReply has the following schema:
     post_id => Integer
     reply_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :post or :reply }
PostSearchData has the following schema:
     post_id => Integer
     search_data => Tsvector
     raw_data => Text
     locale => String
     version => Integer
     __associations => { belongs_to: :post }
PostStat has the following schema:
     id => Integer
     post_id => Integer
     drafts_saved => Integer
     typing_duration_msecs => Integer
     composer_open_duration_msecs => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :post }
PostTiming has the following schema:
     topic_id => Integer
     post_number => Integer
     user_id => Integer
     msecs => Integer
     __associations => { belongs_to: :topic or :user }
PostUpload has the following schema:
     id => Integer
     post_id => Integer
     upload_id => Integer
     __associations => { belongs_to: :post or :upload }
QuotedPost has the following schema:
     id => Integer
     post_id => Integer
     quoted_post_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :post or :quoted_post }
RemoteTheme has the following schema:
     id => Integer
     remote_url => String
     remote_version => String
     local_version => String
     about_url => String
     license_url => String
     commits_behind => Integer
     remote_updated_at => Datetime
     created_at => Datetime
     updated_at => Datetime
     __associations => { has_one: :theme }
SchedulerStat has the following schema:
     id => Integer
     name => String
     hostname => String
     pid => Integer
     duration_ms => Integer
     live_slots_start => Integer
     live_slots_finish => Integer
     started_at => Datetime
     success => true or false
     error => Text
     __associations => {  }
ScreenedEmail has the following schema:
     id => Integer
     email => String
     action_type => Integer
     match_count => Integer
     last_match_at => Datetime
     created_at => Datetime
     updated_at => Datetime
     ip_address => Inet
     __associations => {  }
ScreenedIpAddress has the following schema:
     id => Integer
     ip_address => Inet
     action_type => Integer
     match_count => Integer
     last_match_at => Datetime
     created_at => Datetime
     updated_at => Datetime
     __associations => {  }
ScreenedUrl has the following schema:
     id => Integer
     url => String
     domain => String
     action_type => Integer
     match_count => Integer
     last_match_at => Datetime
     created_at => Datetime
     updated_at => Datetime
     ip_address => Inet
     __associations => {  }
SearchLog has the following schema:
     id => Integer
     term => String
     user_id => Integer
     ip_address => Inet
     search_result_id => Integer
     search_type => Integer
     created_at => Datetime
     search_result_type => Integer
     __associations => {  }
SingleSignOnRecord has the following schema:
     id => Integer
     user_id => Integer
     external_id => String
     last_payload => Text
     created_at => Datetime
     updated_at => Datetime
     external_username => String
     external_email => String
     external_name => String
     external_avatar_url => String
     __associations => { belongs_to: :user }
StylesheetCache has the following schema:
     id => Integer
     target => String
     digest => String
     content => Text
     created_at => Datetime
     updated_at => Datetime
     theme_id => Integer
     source_map => Text
     __associations => {  }
Tag has the following schema:
     id => Integer
     name => String
     topic_count => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { has_one: :tag_search_data, has_many: :tag_users or :topic_tags or :topics or :category_tags or :categories or :tag_group_memberships or :tag_groups }
TagGroup has the following schema:
     id => Integer
     name => String
     created_at => Datetime
     updated_at => Datetime
     parent_tag_id => Integer
     one_per_topic => true or false
     __associations => { has_many: :tag_group_memberships or :tags or :category_tag_groups or :categories, belongs_to: :parent_tag }
TagGroupMembership has the following schema:
     id => Integer
     tag_id => Integer
     tag_group_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :tag or :tag_group }
TagSearchData has the following schema:
     tag_id => Integer
     search_data => Tsvector
     raw_data => Text
     locale => Text
     version => Integer
     __associations => { belongs_to: :tag }
TagUser has the following schema:
     id => Integer
     tag_id => Integer
     user_id => Integer
     notification_level => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :tag or :user }
Theme has the following schema:
     id => Integer
     name => String
     user_id => Integer
     key => String
     created_at => Datetime
     updated_at => Datetime
     compiler_version => Integer
     user_selectable => true or false
     hidden => true or false
     color_scheme_id => Integer
     remote_theme_id => Integer
     __associations => { belongs_to: :color_scheme or :remote_theme, has_many: :theme_fields or :child_theme_relation or :child_themes or :color_schemes }
ThemeField has the following schema:
     id => Integer
     theme_id => Integer
     target_id => Integer
     name => String
     value => Text
     value_baked => Text
     created_at => Datetime
     updated_at => Datetime
     compiler_version => Integer
     error => String
     upload_id => Integer
     type_id => Integer
     __associations => { belongs_to: :upload or :theme }
TopicAllowedGroup has the following schema:
     id => Integer
     group_id => Integer
     topic_id => Integer
     __associations => { belongs_to: :topic or :group }
TopicAllowedUser has the following schema:
     id => Integer
     user_id => Integer
     topic_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :topic or :user }
TopicCustomField has the following schema:
     id => Integer
     topic_id => Integer
     name => String
     value => Text
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :topic }
TopicEmbed has the following schema:
     id => Integer
     topic_id => Integer
     post_id => Integer
     embed_url => String
     content_sha1 => String
     created_at => Datetime
     updated_at => Datetime
     deleted_at => Datetime
     deleted_by_id => Integer
     __associations => { belongs_to: :deleted_by or :topic or :post }
TopicInvite has the following schema:
     id => Integer
     topic_id => Integer
     invite_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :topic or :invite }
TopicLinkClick has the following schema:
     id => Integer
     topic_link_id => Integer
     user_id => Integer
     created_at => Datetime
     updated_at => Datetime
     ip_address => Inet
     __associations => { belongs_to: :topic_link or :user }
TopicSearchData has the following schema:
     topic_id => Integer
     raw_data => Text
     locale => String
     search_data => Tsvector
     version => Integer
     __associations => { belongs_to: :topic }
TopicTag has the following schema:
     id => Integer
     topic_id => Integer
     tag_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :topic or :tag }
TopicTimer has the following schema:
     id => Integer
     execute_at => Datetime
     status_type => Integer
     user_id => Integer
     topic_id => Integer
     based_on_last_post => true or false
     deleted_at => Datetime
     deleted_by_id => Integer
     created_at => Datetime
     updated_at => Datetime
     category_id => Integer
     public_type => true or false
     __associations => { belongs_to: :deleted_by or :user or :topic or :category }
TopicViewItem has the following schema:
     topic_id => Integer
     viewed_at => Date
     user_id => Integer
     ip_address => Inet
     __associations => { belongs_to: :user }
TranslationOverride has the following schema:
     id => Integer
     locale => String
     translation_key => String
     value => String
     created_at => Datetime
     updated_at => Datetime
     compiled_js => Text
     __associations => {  }
TwitterUserInfo has the following schema:
     id => Integer
     user_id => Integer
     screen_name => String
     twitter_user_id => Integer
     created_at => Datetime
     updated_at => Datetime
     email => String
     __associations => { belongs_to: :user }
UnsubscribeKey has the following schema:
     key => String
     user_id => Integer
     created_at => Datetime
     updated_at => Datetime
     unsubscribe_key_type => String
     topic_id => Integer
     post_id => Integer
     __associations => { belongs_to: :user or :post or :topic }
Upload has the following schema:
     id => Integer
     user_id => Integer
     original_filename => String
     filesize => Integer
     width => Integer
     height => Integer
     url => String
     created_at => Datetime
     updated_at => Datetime
     sha1 => String
     origin => String
     retain_hours => Integer
     extension => String
     __associations => { belongs_to: :user, has_many: :post_uploads or :posts or :optimized_images }
UserApiKey has the following schema:
     id => Integer
     user_id => Integer
     client_id => String
     key => String
     application_name => String
     push_url => String
     created_at => Datetime
     updated_at => Datetime
     revoked_at => Datetime
     scopes => Text
     __associations => { belongs_to: :user }
UserArchivedMessage has the following schema:
     id => Integer
     user_id => Integer
     topic_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :user or :topic }
UserAuthToken has the following schema:
     id => Integer
     user_id => Integer
     auth_token => String
     prev_auth_token => String
     user_agent => String
     auth_token_seen => true or false
     legacy => true or false
     client_ip => Inet
     rotated_at => Datetime
     created_at => Datetime
     updated_at => Datetime
     seen_at => Datetime
     __associations => { belongs_to: :user }
UserAuthTokenLog has the following schema:
     id => Integer
     action => String
     user_auth_token_id => Integer
     user_id => Integer
     client_ip => Inet
     user_agent => String
     auth_token => String
     created_at => Datetime
     path => String
     __associations => {  }
UserAvatar has the following schema:
     id => Integer
     user_id => Integer
     custom_upload_id => Integer
     gravatar_upload_id => Integer
     last_gravatar_download_attempt => Datetime
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :user or :gravatar_upload or :custom_upload }
UserBadge has the following schema:
     id => Integer
     badge_id => Integer
     user_id => Integer
     granted_at => Datetime
     granted_by_id => Integer
     post_id => Integer
     notification_id => Integer
     seq => Integer
     __associations => { belongs_to: :badge or :user or :granted_by or :notification or :post }
UserCustomField has the following schema:
     id => Integer
     user_id => Integer
     name => String
     value => Text
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :user }
UserEmail has the following schema:
     id => Integer
     user_id => Integer
     email => String
     primary => true or false
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :user }
UserExport has the following schema:
     id => Integer
     file_name => String
     user_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => {  }
UserFieldOption has the following schema:
     id => Integer
     user_field_id => Integer
     value => String
     created_at => Datetime
     updated_at => Datetime
     __associations => {  }
UserHistory has the following schema:
     id => Integer
     action => Integer
     acting_user_id => Integer
     target_user_id => Integer
     details => Text
     created_at => Datetime
     updated_at => Datetime
     context => String
     ip_address => String
     email => String
     subject => Text
     previous_value => Text
     new_value => Text
     topic_id => Integer
     admin_only => true or false
     post_id => Integer
     custom_type => String
     category_id => Integer
     __associations => { belongs_to: :acting_user or :target_user or :post or :topic or :category }
UserOpenId has the following schema:
     id => Integer
     user_id => Integer
     email => String
     url => String
     created_at => Datetime
     updated_at => Datetime
     active => true or false
     __associations => { belongs_to: :user }
UserOption has the following schema:
     user_id => Integer
     email_always => true or false
     mailing_list_mode => true or false
     email_digests => true or false
     email_direct => true or false
     email_private_messages => true or false
     external_links_in_new_tab => true or false
     enable_quoting => true or false
     dynamic_favicon => true or false
     disable_jump_reply => true or false
     automatically_unpin_topics => true or false
     digest_after_minutes => Integer
     auto_track_topics_after_msecs => Integer
     new_topic_duration_minutes => Integer
     last_redirected_to_top_at => Datetime
     email_previous_replies => Integer
     email_in_reply_to => true or false
     like_notification_frequency => Integer
     mailing_list_mode_frequency => Integer
     include_tl0_in_digests => true or false
     notification_level_when_replying => Integer
     theme_key => String
     theme_key_seq => Integer
     allow_private_messages => true or false
     homepage_id => Integer
     __associations => { belongs_to: :user }
UserProfile has the following schema:
     user_id => Integer
     location => String
     website => String
     bio_raw => Text
     bio_cooked => Text
     profile_background => String
     dismissed_banner_key => Integer
     bio_cooked_version => Integer
     badge_granted_title => true or false
     card_background => String
     card_image_badge_id => Integer
     views => Integer
     __associations => { belongs_to: :user or :card_image_badge, has_many: :user_profile_views }
UserProfileView has the following schema:
     id => Integer
     user_profile_id => Integer
     viewed_at => Datetime
     ip_address => Inet
     user_id => Integer
     __associations => { belongs_to: :user_profile }
UserSearchData has the following schema:
     user_id => Integer
     search_data => Tsvector
     raw_data => Text
     locale => Text
     version => Integer
     __associations => { belongs_to: :user }
UserStat has the following schema:
     user_id => Integer
     topics_entered => Integer
     time_read => Integer
     days_visited => Integer
     posts_read_count => Integer
     likes_given => Integer
     likes_received => Integer
     topic_reply_count => Integer
     new_since => Datetime
     read_faq => Datetime
     first_post_created_at => Datetime
     post_count => Integer
     topic_count => Integer
     bounce_score => Integer
     reset_bounce_score_after => Datetime
     __associations => { belongs_to: :user }
UserVisit has the following schema:
     id => Integer
     user_id => Integer
     visited_at => Date
     posts_read => Integer
     mobile => true or false
     time_read => Integer
     __associations => {  }
UserWarning has the following schema:
     id => Integer
     topic_id => Integer
     user_id => Integer
     created_by_id => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :user or :topic or :created_by }
WatchedWord has the following schema:
     id => Integer
     word => String
     action => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => {  }
WebHookEvent has the following schema:
     id => Integer
     web_hook_id => Integer
     headers => String
     payload => Text
     status => Integer
     response_headers => String
     response_body => Text
     duration => Integer
     created_at => Datetime
     updated_at => Datetime
     __associations => { belongs_to: :web_hook }
WebHookEventType has the following schema:
     id => Integer
     name => String
     __associations => { has_and_belongs_to_many: :web_hooks }
WebHookEventType::HABTM_WebHooks has the following schema:
     web_hook_id => Integer
     web_hook_event_type_id => Integer
     __associations => { belongs_to: :left_side or :web_hook }
